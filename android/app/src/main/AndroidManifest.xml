<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Only used for the student's own voice-practice recordings (role-play,
         handoff practice). Requested in-app with rationale before the system
         dialog — see ui/components/VoiceRecordCard.kt. Nothing is uploaded;
         files stay in app-private storage until the learner deletes them. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.CaregiverProCA">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.CaregiverProCA">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
